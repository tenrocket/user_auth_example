= form_for @item, url: wishlist_items_path do |f|
	= f.hidden_field :wishlist_id, value: params[:wishlist_id]
	= f.label :name
	= f.text_field :name
	= f.label :description
	= f.text_field :description
	= f.submit "Add Item"